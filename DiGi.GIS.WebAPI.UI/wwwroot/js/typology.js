/**
 * Typology definition page.
 *
 * #13 ships the shell only, so this module is an empty bootstrap: #14 fills Available Columns,
 * #15 Selected Columns, #16 Column Properties, #17 Import/Export and #18 the Load modal. One
 * object rather than loose globals: names such as import() or load() are too general to own at
 * window scope (same reasoning as user.js).
 */
const digiTypology = (function () {
    'use strict';

    return {
    };
})();
